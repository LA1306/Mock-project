export type AddressType = {
  detail: string,
  location_lat: string,
  location_long: string,
  province_id: number,
  district_id: number,
  ward_id: number,
  province: string,
  district: string,
  ward: string,
}